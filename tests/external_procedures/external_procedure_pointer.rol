proc main() {
   when sizeof$<usize>() == 4 {
      let x = &fd_write;
      let out = 0;
      x(0, 0 transmute &ciovec, 0, &out);
   } else {
      let x = &syscall1;
      x(12, 0);
   }
   println("ok");
}
__END__
run:
ok
